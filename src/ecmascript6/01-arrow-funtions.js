//funcion convencional
function multiplicar(num) {
    return num + num;
}

//Funcion tipo flecha
const multiplicar = (num) => {
    return num + num;
}

//funcion tipo flecha simplificada
const multiplicar = num => num + num;

/* Una expresión de función flecha es una alternativa 
compacta a una expresión de función tradicional, 
pero es limitada y no se puede utilizar en todas las situaciones. */